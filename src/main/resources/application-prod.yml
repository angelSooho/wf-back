spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${custom.db.url}
    username: ${custom.db.username}
    password: ${custom.db.password}
  jpa:
    hibernate:
      ddl-auto: create

custom:
  redis:
    host: redis_1
    port: 6379